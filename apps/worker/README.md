# Worker

Cloudflare Worker deployed into the user's Cloudflare account.

Responsibilities:

- device pairing
- approval session creation
- push notification dispatch
- audit writes
- encrypted vault record API
